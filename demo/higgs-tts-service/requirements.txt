# Higgs Audio v3 TTS 推理服务 依赖
# pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
requests>=2.28.0

# SGLang-Omni（Higgs v3 后端，不在 PyPI，从 GitHub 安装）
# pip install git+https://github.com/sgl-project/sglang-omni.git
# 如果 GitHub 连不上，clone 源码后 pip install /path/to/sglang-omni/
