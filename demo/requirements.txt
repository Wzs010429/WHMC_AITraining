# ============================================================
# WHMC 工作坊 统一 Python 环境
# 覆盖 FLUX 文生图 + Higgs TTS 两个服务
# ============================================================
# 安装：
#   source demo-env/bin/activate
#   pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
#   pip install sglang-omni

# ── Web 框架（共用）──────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
requests>=2.28.0

# ── FLUX 文生图 ──────────────────────────────────
diffusers>=0.32.0
transformers>=4.46.0
accelerate>=0.30.0
Pillow>=10.0.0
safetensors>=0.4.0
modelscope>=1.20.0

# ── Higgs TTS ────────────────────────────────────
# sglang-omni          ← 单独 pip install（见上方注释）
# torch, torchaudio    ← 单独 pip install（CUDA 版本）

# ── 工具 ─────────────────────────────────────────
huggingface_hub>=1.0.0
